{{- if .Values.networkPolicy.enabled }}
---
# Network Policy for main application
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-app
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.app.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.app.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from realtime service
  {{- if .Values.realtime.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow ingress from cron pods (every cron job curls /api/schedules/execute,
  # webhook polls, etc. against the app service)
  {{- if .Values.cronjobs.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.selectorLabels" . | nindent 10 }}
          simstudio.ai/component-group: cronjob
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow ingress from ingress controller (configurable peers; defaults to any)
  {{- if .Values.ingress.enabled }}
  - from:
      {{- toYaml (default (list (dict)) .Values.networkPolicy.ingressFrom) | nindent 6 }}
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow custom ingress rules
  {{- with .Values.networkPolicy.ingress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  egress:
  # Allow egress to PostgreSQL
  {{- if .Values.postgresql.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.postgresql.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow egress to realtime service
  {{- if .Values.realtime.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  {{- end }}
  # Allow egress to owned Mothership service
  {{- if .Values.mothership.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.mothership.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.mothership.server.service.targetPort }}
  {{- end }}
  # Allow egress to Ollama
  {{- if .Values.ollama.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.ollama.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.ollama.service.targetPort }}
  {{- end }}
  # Allow egress to OpenTelemetry collector (OTLP gRPC + HTTP)
  {{- if .Values.telemetry.enabled }}
  - to:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: {{ include "sim.name" . }}
          app.kubernetes.io/instance: {{ .Release.Name }}
          app.kubernetes.io/component: telemetry
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for external APIs (excludes cloud metadata endpoints)
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
          {{- range (default (list "169.254.169.254/32" "169.254.170.2/32") .Values.networkPolicy.egressExceptCidrs) }}
          - {{ . | quote }}
          {{- end }}
    ports:
    - protocol: TCP
      port: 443
  # Allow custom egress rules
  {{- with .Values.networkPolicy.egress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}

{{- if .Values.realtime.enabled }}
---
# Network Policy for realtime service
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-realtime
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.realtime.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.realtime.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  # Allow ingress from ingress controller (configurable peers; defaults to any)
  {{- if .Values.ingress.enabled }}
  - from:
      {{- toYaml (default (list (dict)) .Values.networkPolicy.ingressFrom) | nindent 6 }}
    ports:
    - protocol: TCP
      port: {{ .Values.realtime.service.targetPort }}
  {{- end }}
  egress:
  # Allow egress to PostgreSQL
  {{- if .Values.postgresql.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.postgresql.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow egress to OpenTelemetry collector (OTLP gRPC + HTTP)
  {{- if .Values.telemetry.enabled }}
  - to:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: {{ include "sim.name" . }}
          app.kubernetes.io/instance: {{ .Release.Name }}
          app.kubernetes.io/component: telemetry
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for external APIs (excludes cloud metadata endpoints)
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
          {{- range (default (list "169.254.169.254/32" "169.254.170.2/32") .Values.networkPolicy.egressExceptCidrs) }}
          - {{ . | quote }}
          {{- end }}
    ports:
    - protocol: TCP
      port: 443
  # Allow custom egress rules
  {{- with .Values.networkPolicy.egress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}

{{- if .Values.mothership.enabled }}
---
# Network Policy for owned Mothership service
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-mothership
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.mothership.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.mothership.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow runtime/admin calls from the main application
  {{- if .Values.app.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.mothership.server.service.targetPort }}
  {{- end }}
  # Allow app CronJobs to call the owned service
  {{- if .Values.cronjobs.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.selectorLabels" . | nindent 10 }}
          simstudio.ai/component-group: cronjob
    ports:
    - protocol: TCP
      port: {{ .Values.mothership.server.service.targetPort }}
  {{- end }}
  # Allow Mothership processor CronJobs to call the owned service
  {{- if .Values.mothership.processor.billingCallbacks.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.mothership.processor.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.mothership.server.service.targetPort }}
  {{- end }}
  egress:
  # Allow callback egress to the main application when SIM_BASE_URL is internal
  {{- if .Values.app.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.app.service.targetPort }}
  {{- end }}
  # Allow egress to PostgreSQL
  {{- if .Values.postgresql.enabled }}
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.postgresql.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow egress to OpenTelemetry collector (OTLP gRPC + HTTP)
  {{- if .Values.telemetry.enabled }}
  - to:
    - podSelector:
        matchLabels:
          app.kubernetes.io/name: {{ include "sim.name" . }}
          app.kubernetes.io/instance: {{ .Release.Name }}
          app.kubernetes.io/component: telemetry
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for provider APIs and public Sim callback URLs
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
          {{- range (default (list "169.254.169.254/32" "169.254.170.2/32") .Values.networkPolicy.egressExceptCidrs) }}
          - {{ . | quote }}
          {{- end }}
    ports:
    - protocol: TCP
      port: 443
  # Allow custom egress rules
  {{- with .Values.networkPolicy.egress }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}

{{- if and .Values.mothership.processor.billingCallbacks.enabled .Values.networkPolicy.enabled }}
---
# Network Policy for owned Mothership billing processor CronJobs
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-mothership-billing-processor
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.mothership.processor.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.mothership.processor.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Egress
  egress:
  # Allow processor calls to the owned Mothership admin endpoint
  - to:
    - podSelector:
        matchLabels:
          {{- include "sim.mothership.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.mothership.server.service.targetPort }}
  # Allow DNS resolution for the Mothership service name
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
{{- end }}

{{- if .Values.postgresql.enabled }}
---
# Network Policy for PostgreSQL
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-postgresql
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.postgresql.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.postgresql.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  # Allow ingress from realtime service
  {{- if .Values.realtime.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow ingress from owned Mothership service
  {{- if .Values.mothership.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.mothership.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  # Allow ingress from migrations job
  {{- if .Values.migrations.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.migrations.labels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.postgresql.service.targetPort }}
  {{- end }}
  egress:
  # Allow minimal egress (for health checks, etc.)
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
{{- end }}

{{- if .Values.ollama.enabled }}
---
# Network Policy for Ollama
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-ollama
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.ollama.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "sim.ollama.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # Allow ingress from main application
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: {{ .Values.ollama.service.targetPort }}
  egress:
  # Allow DNS resolution
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # Allow HTTPS egress for model downloads (excludes cloud metadata endpoints)
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
          {{- range (default (list "169.254.169.254/32" "169.254.170.2/32") .Values.networkPolicy.egressExceptCidrs) }}
          - {{ . | quote }}
          {{- end }}
    ports:
    - protocol: TCP
      port: 443
{{- end }}

{{- if .Values.telemetry.enabled }}
---
# Network Policy for OpenTelemetry Collector
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "sim.fullname" . }}-otel-collector
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "sim.labels" . | nindent 4 }}
    app.kubernetes.io/component: telemetry
spec:
  podSelector:
    matchLabels:
      {{- include "sim.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: telemetry
  policyTypes:
  - Ingress
  - Egress
  ingress:
  # OTLP from app
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.app.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  # OTLP from realtime
  {{- if .Values.realtime.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.realtime.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  # OTLP from copilot
  {{- if .Values.copilot.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.selectorLabels" . | nindent 10 }}
          app.kubernetes.io/component: copilot
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  # OTLP from owned Mothership
  {{- if .Values.mothership.enabled }}
  - from:
    - podSelector:
        matchLabels:
          {{- include "sim.mothership.selectorLabels" . | nindent 10 }}
    ports:
    - protocol: TCP
      port: 4317
    - protocol: TCP
      port: 4318
  {{- end }}
  egress:
  # DNS
  - to: []
    ports:
    - protocol: UDP
      port: 53
    - protocol: TCP
      port: 53
  # HTTPS for forwarding to external observability backends (Datadog, Honeycomb, etc.)
  - to:
    - ipBlock:
        cidr: 0.0.0.0/0
        except:
          {{- range (default (list "169.254.169.254/32" "169.254.170.2/32") .Values.networkPolicy.egressExceptCidrs) }}
          - {{ . | quote }}
          {{- end }}
    ports:
    - protocol: TCP
      port: 443
{{- end }}

{{- /*
  Copilot + copilot-postgresql intentionally do NOT ship dedicated NetworkPolicies.
  Copilot requires REDIS_URL (external Redis on a non-443 port), and the chart
  cannot know the user's Redis host/port at render time — a default egress rule
  would silently block Redis on most installs. Users running networkPolicy.enabled=true
  with copilot enabled should add their own NPs (or extend networkPolicy.egress
  with the appropriate egress rules).
*/}}
{{- end }}
