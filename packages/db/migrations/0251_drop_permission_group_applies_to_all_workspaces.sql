-- migration-safe: `applies_to_all_workspaces` was a derived mirror of `is_default` (create/update routes kept them equal; migration 0246 backfilled legacy rows) that no enforcement/execution path ever read — resolveWorkspaceGroup inner-joins permission_group_workspace and treats org-wide as definitionally `is_default`. All readers/writers are removed in this same change and only admin permission-group management routes touched it, so the sole exposure is a brief deploy-cutover window on those admin routes — accepted (no execution-path impact).
ALTER TABLE "permission_group" DROP COLUMN "applies_to_all_workspaces";